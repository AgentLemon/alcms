namespace :alcms do
  task :install do
    print "installing alcms..."
    # application_helper.rb << include Alcms::ApplicationHelper
    # routes.rb << mount Alcms::Engine => '/alcms'
    # create config/initializers/alcms.rb
  end
end
